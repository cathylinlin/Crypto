# Crypto  
感谢DexterJie师傅的wp。   
## Day 1  
### 通往哈希的旅程     
题目给了一个188开头的11位数字的哈希值，要我们提交数字。   

好的吧，其实当时看了一眼，然后找在线网站爆破了，但是没有爆出来...  
然后找AI要脚本爆破了，多核爆破了20分钟可还行。   
```python
import hashlib
import string
import itertools

c = "ca12fd8250972ec363a16593356abb1f3cf3a16d"

for i in itertools.product(string.digits,repeat=8):
    tmp = ''.join(i)
    t = "188" + tmp
    if hashlib.sha1(t.encode()).hexdigest() ==c:
        print(t)
```
脚本来自DexterJie师傅。   
`itertools.product(string.digits, repeat=8)`会生成所有长度为8的数字串的所有组合。对于每一个这样的组合：`''.join(i)`将当前组合转换成一个连续的字符串。  

### 你是小哈斯吗  
题目给了一个文件，文件里面都是sha1码，尝试找出里面的信息。  

当时我居然可以每行都复制粘贴到在线网站转化一下，后来题目被杀爆的时候我才打算弄个脚本帮我处理一下。   

脚本很简单。我先构建单个字符到其sha1值的字典，键选择sha1值，然后读取文件全部转换成单个字符即可。   
这似乎有点麻烦。有的脚本选择先读取文件每一行，然后对其对应的字符进行爆破。   

```python
import hashlib

def create_hash_dict():
    """Create a dictionary mapping SHA-1 hashes to single ASCII printable characters."""
    hash_dict = {}
    for char in string.printable[:-5]:  # Exclude non-printable characters at the end of string.printable
        sha1_hash = hashlib.sha1(char.encode()).hexdigest()
        if sha1_hash not in hash_dict:
            hash_dict[sha1_hash] = char
    return hash_dict

def reverse_sha1(file_path):
    """Read SHA-1 hashes from file and attempt to reverse them to original characters."""
    hash_dict = create_hash_dict()
    try:
        with open(file_path, 'r', encoding='utf-8') as f:
            lines = f.readlines()
            for line in lines:
                line = line.strip()  # Remove any trailing whitespace or newline characters
                if line in hash_dict:
                    print(hash_dict[line])
                else:
                    print("No")
    except FileNotFoundError:
        print(f"File {file_path} not found.")

# Replace 'hashes.txt' with the path to your file containing SHA-1 hashes.
reverse_sha1('hashes.txt')
```
AI生成。  

### RSA1  
当时一直在看这个题，但一直没做出来。看了wp才恍然大悟。  

`e`是随机128比特位素数。`N=pq`,常规RSA变体。    
flag是被填充的，转数字后是 $m_1$ ,然后flag每个字符都在ASCII表上向后顺移3位，转数字后是 $m_2$.  
题目给了 $c_1\equiv {(em_1)}^{2835}\pmod N;c_2\equiv {m_2}^{2025}\pmod N;c_3= {m_2}^{2835}\pmod N+e$   
 
怎么下手？当时我想把`e`求出来，经过一番代数变形得到，${(c_3-e)}^5-{c_2}^7\equiv 0\pmod N$   

然后我丢到sympy里面解。好吧，其实根本解不出来。于是这题我就卡在这里了。   

实际上用copper方法.   
```sage
x = PolynomialRing(Zmod(N), 'x').gen()
f = c2**7 - (c3-x)**5
f = f.monic()
res = f.small_roots(X=2**128, beta=1, epsilon=0.05)
e = res[0]
print(e)
```
来自官方wp   
这里`X=2**128`表示要求的根的上界，`beta`表示因子的下界(N**beta),`epsilon`表示根的误差范围  

可是为什么？为什么这样做就可以求出来？  
上个世纪，coppersmith在他的一篇论文中，用格方法给出了在模N意义下求解低阶单变量多项式和二元整系数方程的小根的方法。  
当然非常巧的时，sagemath有small_roots()函数可以直接调用coppersmith方法来求解。   
前者（低阶单变量多项式）我们已经见过许多次了，比如p高位泄露，d高位泄露等等。后者（二元整系数方程）呢?sagemath里面并没有，但在一定条件下可以转化为单变量求解（比如直接舍弃一个单变量，或者找两个变量之间的关系）    
更多coppersmith方法使用例可以看[Pion1eer](https://www.ruanx.net/coppersmith/)   

之后`e`求解出来了。

我们知道`bytes_to_long()`函数256进制转10进制，所以既然`m_2`是文本所有字符在ASCII表上向后顺移3位，那么和原来的(`m_1`)必然相差一定值。   
于是问题来到`c_1`和`c_2`是相关消息攻击。(要注意相关消息攻击得到的`m`是原来`m1`模`N`)     

脚本一把梭
```sage
def gcd(g1, g2):
    while g2:
        g1, g2 = g2, g1 % g2
    return g1.monic()

x = PolynomialRing(Zmod(N), 'x').gen()
g1 = (x*e)**2835 - c1
g2 = (x+dm)**2025 - c2
m1 = int(-gcd(g1, g2)[0])


while 1:
    m1 += N
    flag = long_to_bytes(m1)
    if b'flag{' in flag:
        print(flag)
        break
```
来自官方wp

### EzRSA   
当时看了题目，没什么思路，就放了












